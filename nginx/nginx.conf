user nginx;
worker_processes auto;

events {
  worker_connections 1024;
}


http {
  include /etc/nginx/conf.d/*.conf;
  sendfile on;
  gzip on; # compression des données envoyés au client, moins de bande passante
}
